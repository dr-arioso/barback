# Roadmap

## Phase 1 — Stabilize Barback
- Rename backends → resolvers
- Create stable resolver interface
- Add HTML output
- Add multi-output mode

## Phase 2 — Extract StashKit
- Create separate repo
- Move resolvers into plugins
- Add cache directory + dedupe
- Add log events + timing metrics

## Phase 3 — Enhancements
- Support live camera ingestion
- Run multiple resolvers with majority voting
- Natural-language summaries in HTML reports
- Optional cloud storage for output artifacts
